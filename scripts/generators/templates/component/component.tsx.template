import type {
	JSX,
} from 'react'

import type {
	I{{componentNamePascal}}Props,
} from './{{componentNameKebab}}.types'

const {{componentNamePascal}} = (props: I{{componentNamePascal}}Props): JSX.Element => {
	const {
		children,
	} = props

	return (
		<div>
			{children}
		</div>
	)
}

export default {{componentNamePascal}}
